import { makeStyles } from "@fluentui/react-components";

export const useStyles = makeStyles({
  filters: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: "5px",
    padding: "10px",
  },
  menuBtn: {
    height: "32px",
    padding: 0,
  },
});
